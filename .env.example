# MAIAMARI.STUDIO — Ortam değişkenleri
# Bu dosyayı `.env.local` olarak kopyalayıp değerleri doldur.
# .env.local git'e GİRMEZ (.gitignore'da .env* var).

# --- Veritabanı (Supabase Postgres) ---
# Supabase paneli → Project Settings → Database → Connection string → "Transaction pooler"
# (port 6543). <PASSWORD> kısmını proje şifrenle değiştir.
DATABASE_URL="postgresql://postgres.<PROJECT_REF>:<PASSWORD>@aws-0-<REGION>.pooler.supabase.com:6543/postgres"

# --- Görsel yükleme (Supabase Storage) ---
# Project Settings → API
NEXT_PUBLIC_SUPABASE_URL="https://<PROJECT_REF>.supabase.co"
# service_role anahtarı SADECE server tarafında kullanılır, asla client'a sızmaz.
SUPABASE_SERVICE_ROLE_KEY="<SERVICE_ROLE_KEY>"
SUPABASE_STORAGE_BUCKET="product-images"

# --- Admin paneli girişi ---
# Parola DÜZ METİN saklanmaz; bcrypt hash'i girilir. Hash üretmek için:
#   node -e "console.log(require('bcryptjs').hashSync('<güçlü-parola>', 12))"
# ADMIN_SESSION_SECRET için rastgele uzun bir dize üret.
ADMIN_PASSWORD_HASH="<bcrypt-hash>"
ADMIN_SESSION_SECRET="<rastgele-uzun-dize-en-az-32-karakter>"

# Sipariş sahiplik cookie'sinin imza anahtarı (opsiyonel).
# Boşsa ADMIN_SESSION_SECRET kullanılır; ayrıştırmak istersen doldur.
ORDER_ACCESS_SECRET=""

# --- iyzico Checkout Form ---
# Anahtarlar BOŞKEN ödeme adımı yerel "mock" test paneliyle çalışır.
# Anahtarlar girilince aynı akış otomatik gerçek iyzico'ya geçer:
# initialize → hosted ödeme sayfası → callback (retrieve + imza doğrulama).
# SANDBOX: sandbox-merchant.iyzipay.com → Ayarlar → API anahtarları;
#   IYZICO_BASE_URL="https://sandbox-api.iyzipay.com" (aşağıdaki varsayılan).
# CANLI:   merchant.iyzipay.com anahtarları + IYZICO_BASE_URL="https://api.iyzipay.com".
#   Sandbox ve canlı anahtarlar birbirinin base URL'iyle ÇALIŞMAZ; ikisini
#   birlikte değiştir. Canlı anahtarlar yalnız Vercel Production env'e girilir.
# DİKKAT: callbackUrl public HTTPS olmalı → lokal sandbox testi için SITE_URL'i
# tünel adresine (cloudflared/ngrok) çevir ve akışı hep tünel URL'inden yürüt.
IYZICO_API_KEY=""
IYZICO_SECRET_KEY=""
IYZICO_BASE_URL="https://sandbox-api.iyzipay.com"
# Taksit seçenekleri (virgüllü, örn "1,2,3"). Boş = tek çekim.
IYZICO_INSTALLMENTS=""

# --- Sipariş bildirimi e-postası (Resend) ---
# resend.com'da ücretsiz hesap aç, API Keys'ten bir anahtar oluştur.
# ORDER_EMAIL_FROM: test için onboarding@resend.dev (yalnız hesap e-postana
# gönderir). Prod'da maiamari.art'ı Resend'de doğrulayıp
# "Maiamari <siparis@maiamari.art>" yap. Müşteriye onay maili için
# ORDER_EMAIL_CUSTOMER="1" (doğrulanmış alan gerekir). Anahtar yoksa bildirim
# sessizce atlanır; sipariş akışı etkilenmez.
RESEND_API_KEY=""
ORDER_EMAIL_FROM="Maiamari <onboarding@resend.dev>"
ORDER_EMAIL_TO="info@maiamari.art"
ORDER_EMAIL_CUSTOMER="0"

# Maildeki "Panelde aç" linkinin tabanı. LOKAL testte http://localhost:3000 yap;
# prod'da (Vercel) boş bırak → varsayılan https://www.maiamari.art.
SITE_URL=""

# Checkout (sepet + kendi ödeme akışı) canlıda açık mı. iyzico GERÇEK ödeme hazır
# olana kadar PROD'da KONMAZ (kapalı → müşteri mock test ödemesine ulaşamaz,
# "Online ödeme yakında" görür). Lokal geliştirme + iyzico hazır olunca: 1.
# DİKKAT: NEXT_PUBLIC_* değişkenleri BUILD ANINDA koda gömülür. Vercel'de bu
# değeri değiştirince build cache'SİZ redeploy gerekir; cache'li redeploy eski
# değeri taşıyabilir.
NEXT_PUBLIC_CHECKOUT_ENABLED=""
